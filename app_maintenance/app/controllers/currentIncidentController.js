const datamapper = require('../datamapper');

const currentIncidentController = {
    getIncidentById: async function (req, res, next){

        
    },

    modifyOneIncident: async function (req, res, next){

        
    }
};

module.exports = currentIncidentController;